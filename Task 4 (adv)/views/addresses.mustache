<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>{{ title }}</title>
</head>
<body class="d-flex flex-column gap-5 m-3">
    <h1>{{ title }}</h1>

        <div class="d-flex flex-column">
        {{#addresses}}
        <ul class="list-group list-group-horizontal">
            <li class="list-group-item">
                <img src="{{img}}" alt="{{name}}" width="450">
            </li>
            <li style="width: 160px;" class="list-group-item d-flex justify-content-center align-items-center">
                <p>{{name}}</p>
            </li>
        </ul>
        {{/addresses}}
        </div>
    
    <a href="/"><button class="btn btn-warning">Главная страница</button></a>

    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>