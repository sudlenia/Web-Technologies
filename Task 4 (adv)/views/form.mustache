<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>{{ title }}</title>
</head>
<body class="d-flex flex-column gap-4 m-3">
    <h1>{{ title }}</h1>

    <p>{{ description }}</p>
    
    <form class="d-flex flex-column gap-1 align-items-start" action="/submit" method="POST">
        <input class="form-control" style="width: 200px;" type="text" name="name" placeholder="{{ name }}">
        <label class="form-check-label" for="category">{{ category }}
            <select class="form-select" name="category">
                <option value="quality">Качество</option>
                <option value="design">Дизайн</option>
                <option value="price">Цена</option>
            </select>
        </label>
        <div class="form-check">
            <input class="form-check-input" type="checkbox" name="agreement">
            <label class="form-check-label">{{ agreement }}</label>
        </div>
        <button class="btn btn-success" type="submit">Отправить</button>
    </form>

    <a href="/"><button class="btn btn-warning">Главная страница</button></a>

    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>